<odoo>
    <data>
        <!-- explicit list view definition -->
        <!--
        <record model="ir.ui.view" id="empleado-car.list">
          <field name="name">empleado-car list</field>
          <field name="model">empleado-car.empleado-car</field>
          <field name="arch" type="xml">
            <tree>
              <field name="name"/>
              <field name="value"/>
              <field name="value2"/>
            </tree>
          </field>
        </record>
        -->

        <!-- actions opening views on models -->
        <!--
        <record model="ir.actions.act_window" id="empleado-car.action_window">
          <field name="name">empleado-car window</field>
          <field name="res_model">empleado-car.empleado-car</field>
          <field name="view_mode">tree,form</field>
        </record>
        -->

        <!-- server action to the one above -->
        <!--
        <record model="ir.actions.server" id="empleado-car.action_server">
          <field name="name">empleado-car server</field>
          <field name="model_id" ref="model_empleado-car_empleado-car"/>
          <field name="state">code</field>
          <field name="code">
            action = {
              "type": "ir.actions.act_window",
              "view_mode": "tree,form",
              "res_model": self._name,
            }
          </field>
        </record>
        -->

        <!-- Top menu item -->
        <!--
        <menuitem name="empleado-car" id="empleado-car.menu_root"/>
        -->
        <!-- menu categories -->
        <!--
        <menuitem name="Menu 1" id="empleado-car.menu_1" parent="empleado-car.menu_root"/>
        <menuitem name="Menu 2" id="empleado-car.menu_2" parent="empleado-car.menu_root"/>
        -->
        <!-- actions -->
        <!--
        <menuitem name="List" id="empleado-car.menu_1_list" parent="empleado-car.menu_1"
                  action="empleado-car.action_window"/>
        <menuitem name="Server to list" id="empleado-car" parent="empleado-car.menu_2"
                  action="empleado-car.action_server"/>
        -->
    </data>
</odoo>